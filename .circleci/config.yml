version: 2.1

setup: true

orbs: 
  # continuation: circleci/continuation@0.2.0
  path: circleci/path-filtering@0.1.3

workflows:
  setup:
    jobs:
      - path/filter:
          config-path: .circleci/next.yml
          mapping: |
            app1/.* build-app1 true
            app2/.* build-app2 true
            app3/.* build-app3 true
